import * as pnpmLockfileLib from '@pnpm/lockfile-file';
import { Dep, Lockfile, ManifestFile, PkgTree } from './index';
import { DepMap, LockParserBase } from './lock-parser-base';
export interface PnpmFileLock extends pnpmLockfileLib.Lockfile {
    type: string;
    dependencies?: pnpmLockfileLib.ResolvedDependencies;
    devDependencies?: pnpmLockfileLib.ResolvedDependencies;
}
export declare class PnpmPackageLockParser extends LockParserBase {
    constructor();
    parseLockFile(lockFileContents: string): PnpmFileLock;
    getDependencyTree(manifestFile: ManifestFile, lockfile: Lockfile, includeDev?: boolean, strict?: boolean, workspace?: string): Promise<PkgTree>;
    getDepMap(lockfile: Lockfile, workspace?: string): DepMap;
    protected getDepTreeKey(dep: Dep): string;
    protected getName(depName: string): string;
    protected getVersion(depName: string): string;
}
